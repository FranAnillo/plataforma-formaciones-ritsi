# En desarrollo local con `pnpm start`, apunta al backend directo.
REACT_APP_BACKEND_URL=http://localhost:8000

# En Docker/despliegue con Nginx, déjalo vacío para usar /api en el mismo origen.
# REACT_APP_BACKEND_URL=
